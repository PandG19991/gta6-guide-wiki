{
  "name": "leonida-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro check && astro build",
    "preview": "astro preview --host 127.0.0.1",
    "deploy": "npm run build && wrangler pages deploy dist --project-name leonida-ledger --branch main"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "astro": "^7.0.6",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.3",
    "wrangler": "^4.93.0"
  }
}
