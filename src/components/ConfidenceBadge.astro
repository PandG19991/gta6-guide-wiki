---
import { confidenceLevels, type ConfidenceKey } from "@data/site";

interface Props {
  value: ConfidenceKey;
}

const { value } = Astro.props;
const confidence = confidenceLevels.find((item) => item.key === value);
---

<span class={`badge ${value}`}>{confidence?.label ?? value}</span>

